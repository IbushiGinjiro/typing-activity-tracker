# KNOWLEDGE.md

## 実装上の気付き

- バースト区切りの閾値とタイポ訂正判定の閾値は、両方とも「直前イベントからの経過時間」で判定している。
  バースト区切り(2秒)の方がタイポ訂正判定(1秒)より大きい値である前提でロジックを組んでいるので、
  設定変更時にこの大小関係を逆転させると分類がおかしくなる(閾値変更時は要注意)。
- pystrayの`radio=True`は同じ`pystray.Menu`インスタンス内のアイテム同士でしか排他表示にならない。
  キーボード切替のサブメニューを毎回`_build_menu()`で作り直しているのはこのため。
- Flaskの開発サーバーをバックグラウンドプロセスとして起動して動作確認する際、bashの`&`で
  取れる`$!`は期待通りのPIDにならないことがあった(ラップされたプロセスのPIDになる場合がある)。
  確実に止めたい場合は`ps aux | grep python`で実プロセスIDを確認してkillする方が確実。
- ダッシュボードのJSONデータをテンプレートに埋め込む際、`json.dumps`はデフォルトで非ASCII文字を
  `\uXXXX`にエスケープするが、これは正しいJSON/JSリテラルなのでブラウザ側では問題なくパースできる。

- タスクトレイ(バックグラウンドスレッド)からtkinterの`simpledialog`を開くと、ダイアログ自体は
  表示されるのにキー入力が別ウィンドウに飛んでしまい、文字が打てない不具合が発生した
  (Windowsが非フォアグラウンドプロセスの新規ウィンドウにキー入力フォーカスを渡さないため)。
  → 「新しいキーボードを追加」はタスクトレイから外し、常にアクティブなブラウザ側の
    ダッシュボード(フォーム)に移すことで解決した。「切替」はマウスクリックのみで完結するため
    タスクトレイ側に残している。
- pystrayの`Menu`はコンストラクタに単一のcallableを渡すと、メニューを開くたびにその関数を呼んで
  項目を作り直してくれる(動的メニュー)。DB側の変更(ダッシュボードでのキーボード追加等)を
  タスクトレイ側が自動的に反映するために使っている。
- Git Bashの`curl`経由で日本語文字列をPOSTすると、ターミナルのコードページの影響で文字化けする
  ことがあった(アプリ側のバグではなく、Windows上のGit Bashのシェル/ターミナルエンコーディングの
  問題)。動作確認は`urllib.request`でUTF-8エンコードして直接POSTする方が確実。

## 調整結果(閾値のチューニング履歴)

- (まだ実運用データなし。v1公開後、実際に使いながらここに調整結果を追記する)

## キーボード別タスクトレイアイコン(2026-09-15)

- pystrayの`Icon.icon`プロパティは実行中でも再代入でき、代入した瞬間にタスクトレイの表示が
  更新される。キーボード切替のコールバック内で`self.icon.icon = 新しい画像`とするだけでよく、
  `icon.update_menu()`のような明示的な再描画呼び出しは不要だった。
- `.piskel`ファイルの実体は「1レイヤー・1フレームのスプライトシートPNGをbase64で埋め込んだJSON」
  (`piskel.layers`が`{"chunks":[{"layout":[[0]],"base64PNG":"data:image/png;base64,..."}]}`を
  文字列化したものの配列)。したがって「PNG生成に使ったのと同じPillow画像をbase64化して埋め込む」
  だけでPNGと.piskelを同じ定義から生成でき、手作業でのズレを防げる。
  - **要注意**: フィールド名は`base64PNG`(単数形・値は文字列)であり、`base64PNGs`(複数形・配列)
    ではない。最初の実装では複数形で書いてしまい、Piskel側の「Import and merge」で開くと
    中身が空(ブランク)になる不具合が発生した。実例(GitHub上の`.piskel`ファイル)で
    フィールド名を確認して修正済み。
- タスクトレイの実表示は16〜24px程度に縮小されるため、実機の精密な模写よりも
  「レイアウト形状のシルエット+キーボードごとの差し色」の方が視認性が良い
  (今回は65%=横長+右ナビ列、40%=小さめ+分割スペースバー、で形状を差別化し、
  ケース色・スペースバー色をキーボードごとに変えた)。
## PyInstallerでの配布ビルドの落とし穴(2026-09-16)

実際に`pyinstaller`でビルドしたexeを、本番と別ポート・別フォルダで動かして確認したところ、
`python -m app.main`での開発時の動作確認だけでは気づけない不具合が2つ見つかった。
「単体テストが通る」「開発時に動く」だけでは配布用ビルドの正しさは保証されないので、
配布判断の前には必ず実際にビルドしたexeを動かして確認すること。

1. **相対importが壊れる**: `app/main.py`を直接PyInstallerのビルド対象にすると、
   PyInstallerがそれを「トップレベルスクリプト」として扱うため、`app`パッケージの一員という
   認識にならず`from .capture import KeyCapture`のような相対importが
   `ImportError: attempted relative import with no known parent package`で失敗する。
   対策: `from app.main import main`のように絶対importする`run.py`をプロジェクト直下に用意し、
   そちらをビルド対象にする。
2. **設定・DBの置き場所が実行のたびに変わる(重大)**: `app/config.py`の`PROJECT_ROOT`を
   `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`のように`__file__`基準で
   決めていたが、PyInstallerのonefileビルドでは`__file__`は**実行のたびに新しく作られる
   一時展開フォルダ**(`sys._MEIPASS`)を指す。気づかずに配布すると、`config.json`が
   exeの隣に置いても読み込まれず、しかも`data/events.db`が**起動のたびにリセットされて
   打鍵データが一切蓄積されない**という、アプリの存在意義に関わる致命的な不具合になる。
   対策: `sys.frozen`(PyInstallerが実行時にセットするフラグ)を見て、frozen時は
   `os.path.dirname(sys.executable)`(exe自体のあるフォルダ)を基準にする。
3. **バックグラウンドスレッドでのCOM(WMI)利用は要`pythoncom.CoInitialize()`**:
   `wmi.WMI()`(COMオブジェクト)をメインスレッドで作って別スレッド(`threading.Thread`)に
   渡すと、実際のビルドで動かした際に
   `pywintypes.com_error: ... SWbemServicesEx ...`→`AttributeError: winmgmts:.Win32_PnPEntity`
   で監視スレッドが起動直後に例外を出して死ぬ(`--windowed`ビルドだとコンソールが無いため、
   この例外は**完全に無言で失敗し、USB自動検出が動いていないことに気づく手段が無い**)。
   対策: `wmi.WMI()`の生成自体を各スレッドの中で行い、そのスレッドの先頭で
   `pythoncom.CoInitialize()`、終了時に`pythoncom.CoUninitialize()`を呼ぶ
   (COMはスレッドごとにアパートメントを初期化する必要があるため)。
   この種の不具合は`tests/test_device_watch.py`のような、`_handle_creation`等を直接呼ぶ
   単体テストでは検出できない(スレッド生成・COM初期化を経由しないため)。実際にビルドした
   exeを動かして初めて見つかった。

## ダッシュボードJSの落とし穴(2026-09-16)

- `dashboard.html`の`<script>`は1つのタグにまとまっているため、途中の処理(Chart.jsでのグラフ描画)が
  キャッチされない例外を投げると、**同じタグ内でそれより後に書かれたコードは実行されずに止まる**
  (JSはタグ単位で同期的に上から実行されるため)。Chart.jsはCDN(cdnjs.cloudflare.com)から読み込んで
  いるので、オフライン・CDN障害時に`Chart`が未定義になり得る。実際にclaude-in-chromeでの動作確認中に
  CDNが503を返し、この問題を実際に再現できた。グラフ描画は必ずtry/catchで囲み、後続の機能
  (期間サマリの読み込み・favicon追従ポーリング等)を道連れにしないようにする。

## USB自動検出機能(2026-09-16)

- `wmi`パッケージの`Win32_PnPEntity.watch_for(notification_type="creation"/"deletion", delay_secs=1, PNPClass="Keyboard")`で、
  キーボード関連デバイスの着脱イベントだけを絞り込んで監視できる。戻り値のwatcherは呼び出し可能オブジェクトで、
  `watcher(timeout_ms=1000)`のようにタイムアウト付きで呼ぶと、タイムアウト時は`wmi.x_wmi_timed_out`を投げる。
  これを使えばブロッキングせずに定期的にstop_eventをチェックできる(スレッド終了処理のため)。
- 実機(ユーザーのPC)で`Win32_PnPEntity`を列挙したところ、`PNPClass == "Keyboard"`でフィルタするだけで、
  USB複合デバイス(キーボード+メディアキー等の複数インターフェースを持つもの)の中から実際の
  キーボードインターフェース1つだけがちょうど拾えた(他のインターフェースは`PNPClass == "HIDClass"`になる)。
  複合デバイスの重複排除を自前で書く必要はなかった。
- `DeviceID`からVID/PIDを取り出す際は、`MI_00`(マルチインターフェース番号)やインスタンスパス
  (`\8&273C3704&0&0000`等、USBポートごとに変わりうる)を含めず、`VID_xxxx&PID_yyyy`部分だけを
  正規表現で抜き出す(`app/device_key.py`)。これで同じ物理キーボードなら挿すたびに同じキーになる。
- **BluetoothキーボードはVID/PIDが取れない場合がある**: 実機で確認できたBLE HIDデバイス
  (`BTHLEDEVICE\{uuid}_<MAC>\...`という形式)は`DeviceID`にVID_/PID_を含んでおらず、
  `extract_device_key()`はNoneを返す = 自動検出の対象外になる。手元に実際のBluetoothキーボードが
  無く検証できていないため、実機確認が必要(クラシックBluetooth HIDなら`BTHENUM\...&VID_xxxx&PID_yyyy...`
  の形式でVID/PIDが載ることもあるらしいので、機種によって挙動が変わる可能性がある)。
- 内蔵ノートPCキーボード(`ACPI\ATK3001\...`等、PS/2やI2C HID経由)も同様にVID/PIDを持たないため
  自動検出の対象外になる。これは想定通りの挙動(内蔵キーボードは常時接続されているので、
  PnPの着脱イベント自体がほぼ発生しない)。
- `winotify`の`Notification(..., launch=url)`で、トーストをクリックした時にブラウザでURLを開ける
  (`webbrowser`を自前で呼ぶ必要はない)。ただしPyInstaller化した無署名exeでのAppUserModelID登録の
  安定性は未検証(配布作業時に実機確認が必要)。
- 打鍵イベント(`events`テーブル)自体に「どのVID/PID・どの経路で切り替わったか」を持たせると
  行数が多くレコードサイズも増えるため、代わりに切替のたびに1行だけ記録する`keyboard_switches`
  テーブルを別に用意した。「ある時刻の打鍵がどのdevice_key/経路のラベルだったか」は、その時刻の
  直前の`keyboard_switches`行を引けば分かるので、事後訂正の目的には十分。
- キーボード自動検出の状態遷移(猶予タイマーのキャンセル/引き継ぎ等)は分岐が多く、頭の中の
  検証だけだと間違えやすい(実際、壁打ち中に一度誤った説明をしてユーザーに指摘された)。
  `tests/test_device_watch.py`のように、実デバイス無しでも`_handle_creation`/`_handle_deletion`を
  直接呼んでシナリオごとにアサートするテストを書くと、設計の矛盾を実装前に機械的に検出できる。

- ユーザーがPiskelで手動編集した`f65.png`/`th40.png`は、`generate_icons.py`の`generate()`を
  無条件で全件再実行すると上書きされてしまう。他のアイコン(`default`等)だけ追加・再生成したい
  場合は、`generate(names=["default"])`のように対象を絞って呼び出すこと(全件再生成は
  「コード側の定義に一括で戻したい」時だけ使う)。
  なお、Piskel側で32x32のまま書き出す(こちらの64x64アップスケールをかけない)と、
  生成時と読み込んだ画像のサイズが変わるが、pystray/PILはサイズ違いでも問題なく表示できるため
  実害はない。
