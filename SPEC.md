# SPEC.md

## 目的
- 職場と自宅で使っているキーボードそれぞれの習熟度(速度・正確さ)を比較したい
- 新しく買い足すキーボードが「標準レベル」まで使えるようになったかの目安にする
- 面白ければ他の人のPCにも配って話のネタにする(将来構想。v1のスコープ外)

## 対象環境
- Windows常駐アプリ(タスクトレイ常駐)

## キーボードの区別
- Raw Input APIによる自動判別は行わない(実装コストが高いため見送り)
- 1日の始めに手動で「現在使用中のキーボード」ラベルを設定する
  - 登録済みラベルへの**切替**: タスクトレイのメニュー(クリックのみ)
  - 新しいラベルの**追加**: ダッシュボード画面のフォーム(タスクトレイのバックグラウンドスレッドから
    tkinterダイアログを開くとWindows側でキー入力フォーカスが渡らない問題があったため)
- そのラベル単位で打鍵ログを集計する

## 計測項目

### 速度
- 打鍵間隔が一定時間(デフォルト2秒、設定可能)空いたら「バースト」の区切りとする
- バーストごとに「打鍵数 ÷ バースト時間」でCPM(文字/分)を算出
- Backspace/Deleteは区切り判定には使うが、速度算出の分母には含めない版・含める版の両方を保持する
- 日次サマリでは、全バーストの単純平均ではなく中央値を主指標にする(長考の混入を防ぐため)

### 正確さ
- Backspace/Delete発生時、直前の打鍵からの経過時間で分類する
  - 短時間(デフォルト1秒以内)→ 「タイポ訂正」としてカウント
  - それ以上の間隔 → 「言い回しの推敲」とみなし、訂正率の分子には含めない(別集計はする)
- 訂正率 = タイポ訂正回数 ÷ 総打鍵数(バースト単位・日次単位の両方で算出)

### 記録する情報 / しない情報
- 記録する: キーコード種別(通常キー/Backspace/Delete等の種別のみ)、押下タイムスタンプ、現在選択中のキーボードラベル
- 記録しない: 実際に入力された文字列や単語の内容(プライバシー配慮、キーロガーにしないため)

## 出力
- ログ: ローカルに打鍵イベント(またはバースト単位の集計)を保存(SQLite等)
- ダッシュボード: ローカルで見られるHTML/Web画面で、日次・週次のトレンドとキーボード別の比較を表示

## 技術スタックの提案
dev/配下の他ツール(Python + 単一HTMLダッシュボードの構成が多い)との親和性を考えると、以下を提案します。
- 常駐部分: Python + `pynput`(グローバルキーフック) + `pystray`(タスクトレイアイコン)
- データ保存: SQLite(ファイル1つで完結、後から集計しやすい)
- ダッシュボード: ローカルHTTPサーバ(Flask等)でブラウザ表示、または集計結果から静的HTMLを生成して開く方式
- 配布: PyInstallerで単一の.exeにビルドし、GitHub Releasesからダウンロードできるようにする

## しきい値(v1は仮値、後で調整可能な前提)
- バースト区切り: 2秒
- タイポ訂正の判定: 直前打鍵から1秒以内のBackspace/Delete

## 将来の公開配布について
- 別途、公開用のGitHubリポジトリ(このプロジェクト単体)を作り、そこにpushして公開する想定
- グローバルキーフックを使うアプリはWindows Defender/SmartScreenやウイルス対策ソフトに「キーロガー」として誤検知されやすいため、README等で「実際に入力された文字列は記録しない」ことを明示し、ソースコードを公開して透明性で担保する方針とする
- コード署名は個人配布ではハードルが高いためv1では見送り

## v1スコープ外(将来構想)
- 他PCへの配布・インストーラ化の実運用(GitHub Releases公開自体は上記の通り視野に入れるが、実際のビルド・公開作業はv1完成後)
- Raw Input APIによるキーボード自動判別
- アプリケーションごと(コーディング中/文章作成中など)の内訳

---

## 追加機能: キーボード別タスクトレイアイコン(2026-09-15)

### 目的
現在選択中のキーボードラベルに応じて、タスクトレイのアイコン画像を切り替える。
一目で「今どのキーボードとして記録中か」が分かるようにする。

### 対象キーボード
現在DBに登録済みの2台+デフォルトの計3種。
- 「職場aula65%」→ EPOMAKER x AULA F65(65%レイアウト、67キー、白に近いグレー/ブルー系の配色)
- 「自宅TH40%」→ EPOMAKER TH40(40%レイアウト、44キー、分割スペースバー、赤みがかったグレー/赤紫系の配色。
  ブラック/ゴールドは別バリエーションのため今回は使わない)
- 「default」(未切替時の初期状態)→ ASUS Vivobook(黒っぽいノートPC)のキーボード面+タッチパッドを
  上から見た構図。当初の青丸+"T"アイコンから差し替え(2026-09-15)
- 上記3つ以外の未登録・将来追加されるラベル→ 「default」用アイコンにフォールバック
  (`app/tray.py`の`DEFAULT_ICON_FILE`)

### 絵柄の方向性
- タスクトレイの実表示は16〜24px程度に縮小されるため、精細な絵は潰れる。
- そのため「実機をそのまま模写」ではなく、**レイアウト形状のシルエット+キーボードごとの差し色**で
  ドット絵(64x64px、既存の`_make_icon_image`と同サイズ)を新規に描き起こす。
  - 65%(F65): 横長の主要キー領域+右側にナビゲーションキー列がある形状シルエット、白に近いグレー系+ブルーの差し色
  - 40%(TH40): 65%よりひと回り小さい正方形に近い形状シルエット、下段に分割スペースバーの切れ目、赤みがかったグレー系+赤紫の差し色
- 実機の写真・製品画像はそのまま使わず(著作権・利用規約への配慮)、上記のレイアウト特徴を参考に
  独自のドット絵として新規作成する。

### 実装方式
- キーボードラベル→アイコンファイル名の対応表を`app/config.py`または専用JSONで持つ
  (ラベル名を後から変更しても対応表だけ直せばよく、ファイル名に使えない文字の制約も回避できる)
- アイコン画像は`app/assets/icons/*.png`に配置。生成元のピクセルデータ(色の2次元配列)を
  Pythonスクリプト側に定義し、そこから同時に`.png`(pystray用)と`.piskel`(Piskelエディタで
  ユーザーが直接開いて修正できる形式)を書き出す。両者が同じ定義から生成されるため、
  手作業でのズレが起きない。
- `app/tray.py`の`_make_icon_image()`を拡張し、`state.keyboard_name`に応じて対応するPNGを
  読み込む(対応表に無いラベルは従来のデフォルト生成アイコン)。
- タスクトレイの「キーボードを切替」実行時に、`icon.icon`へ新しい画像を再代入してアイコン表示も
  即座に切り替える(現状はアプリ起動時に1回だけ設定されており、切替時の更新処理が未実装のため
  ここを追加する)。

### 成果物
- `app/assets/icons/default.png` / `f65.png` / `th40.png`(pystray実行時に読み込む)
- `app/assets/icons/piskel/f65.piskel` / `th40.piskel`(ユーザーがPiskelで開いて色・形を修正できる元データ)
- ラベル→アイコンの対応表(コード内 or JSON)
- `tray.py`のアイコン切替ロジック更新

この内容で進めてよいか、確認をお願いします。

---

## 追加機能セット(2026-09-16)

以下4件をまとめて進める。実装順は「1→2→3→4」(小さい順、かつ2は3・4の前提になるため)。

### 1. ブラウザタブのfavicon連動(細かいupdate)

- ダッシュボードの`<title>`左のfaviconを、現在選択中キーボードのタスクトレイアイコン(`app/assets/icons/*.png`)と同じ画像にする。
- アイコンのラベル→ファイル対応表は現在`app/tray.py`の`KEYBOARD_ICON_FILES`にあるが、ダッシュボード側(`app/dashboard.py`)からも同じ対応表を参照する必要があるため、`app/icons.py`(新規)に切り出して両方から import する形にリファクタする。
- Flaskに`/icons/<keyboard_name>.png`のような画像配信ルートを追加し、`dashboard.html`の`<link rel="icon">`をそれ経由にする。
- **タスクトレイ側でキーボードを切り替えた場合でも、開いたままのダッシュボードタブのfaviconが追従する**必要があるため、ページ読み込み後にJSで数秒おきに軽量な`/api/current_keyboard`を叩き、値が変わっていたら`<link rel="icon">`の`href`を書き換える(ポーリング。切替頻度は低いので数秒間隔で十分)。
- 同じ理由で、ダッシュボード(ブラウザ側フォーム)でキーボードを切り替えた場合にも**タスクトレイのアイコンが追従**していない現状のギャップも合わせて直す。`AppState.set_keyboard()`に変更通知の仕組み(コールバック登録)を追加し、`TrayApp`側でそれを購読して`icon.icon`を更新する形にリファクタする(タスクトレイからの切替・ダッシュボードからの切替・自動検出による切替、の3経路すべてで同じ通知経路を通すことで今後も一貫させる)。
- 将来ユーザーが増やす自作キーボードには専用アイコンがまだ無いことが多いので、対応表に無いラベルは`default.png`にフォールバック(タスクトレイと同じ挙動)。

### 2. キーボードの自動選択(大きめupdate)

**識別方法**: WindowsのPnP(プラグアンドプレイ)デバイス情報から、USB/BluetoothキーボードのVID(ベンダーID)・PID(プロダクトID)を取得し、それを「機種の識別キー」として使う(`wmi`パッケージ経由でWMIの`Win32_PnPEntity`を監視し、`PNPClass`がキーボード関連のものの着脱イベントを拾う)。同じ機種を複数台持っていても区別はできない(VID/PIDが同じため)前提とする。

**新しいテーブル**: `device_keyboards(device_key TEXT UNIQUE, keyboard_id INTEGER)` を追加し、「このVID/PIDのキーボードは、この登録済みラベルである」という対応を保存する。

**初回セットアップ(オンボーディング)**:
1. インストール後初回起動時、通常のダッシュボードの代わりに`/onboarding`を表示する(登録済みキーボードが0件、かつオンボーディング未完了フラグの場合。フラグは`config.json`に`onboarding_done: true/false`で保持)。
2. オンボーディング画面には「外部接続キーボードを使わない(ノートPC内蔵のみ)」チェックボックスを用意する。
   - チェック時: `default`ラベルのみで運用開始(自動検出のポップアップも出さない設定にする)。
   - 非チェック時: 現在使っている外部キーボードの名前を入力して最初の1台を登録する。
3. オンボーディング完了で`onboarding_done: true`を保存し、以降は通常のダッシュボードを表示する。

**通常運用時の自動検出**:
- 新しいキーボード(PnPの着信イベント)を検出したら、そのVID/PIDが`device_keyboards`に**登録済みなら**、自動的に該当ラベルへ切替(Windowsトースト通知で「◯◯に切り替えました」と一言だけ知らせる)。
- **未登録の**VID/PIDなら、Windowsトースト通知で「新しいキーボードを検出しました。クリックすると登録できます」を表示し、**クリックするとブラウザのダッシュボードが開き**、`/keyboards/new-device?key=...`のような専用画面が出る。そこには登録済みキーボードのリスト(選択するとそのラベルに紐付け。同じ型番を複数台持っていて意図的に同じラベルにまとめたい場合はここで選ぶ)+末尾に「新規登録」の入力欄(新しいラベルを作って紐付け)を用意する。
  - トースト通知は現状の依存関係(`pystray`単体)ではクリック時にURLを開く機能が無いため、`winotify`パッケージを新規に追加する想定(軽量・Windows 10/11のトースト通知とクリック時アクション/URL起動に対応)。
- オンボーディングで「外部接続キーボードを使わない」を選んだ場合でも、後から外部キーボードを挿せば同じ検出・通知フローが動く(いつでも追加登録できる)。

**前提となる制約(実装方式そのものの限界)**:
- 打鍵イベントの記録(`pynput`のグローバルフック)は、**どの物理キーボードから来た打鍵かを区別できない**。USB自動検出で分かるのは「今どのキーボードが挿さっているか」だけで、「今どっちで打っているか」ではない。したがって本機能は「挿さっているデバイスの変化に合わせて『現在のラベル』という単一のフラグを切り替える」仕組みであり、複数デバイスを同時に使い分けることには対応できない。

**シナリオ(2026-09-16、壁打ちで確定)**:

*1. IDが別々の登録済みキーボードA・Bの場合*
1. Aを初めて挿す → 「新しいキーボードを検知」トースト → 新規登録画面(ブラウザ)→ラベル登録
2. Aを登録済み状態で挿す → 「Aを検知」トースト→自動的にAラベルで記録開始
3. Bも同様(初回は新規登録、以降は自動切替)

*2. 同じVID/PIDだが物理的には別デバイスA・Bの場合(Aのみラベル登録済み)*
1. Aが登録済みの状態でBを挿す → システムからは「Aが挿さった」としか見えないので、「Aを検知」トーストが出る(ここでユーザーが「実は違うキーボードだ」と気づく他ない。システム側に矛盾検知の手段は無い)
2. ユーザーが気づいたら、タスクトレイ or ダッシュボードで手動でBラベルに切り替える

*3. 2台同時に挿さっている場合 → 対応を諦める(明示的にスコープ外とする)*
- 最後にPnPイベントが来た方が問答無用で「現在のラベル」を奪う(充電目的で挿しただけでも切り替わってしまう)、PC起動時に両方挿さっていた場合はどちらが先に検知されるか不定、といった不安定さがあるため、複数デバイス同時使用の面倒は見ない。1台ずつ使う運用を前提とする。

*4. 抜いた場合(defaultへのフォールバック、猶予期間あり)*
- 「現在のラベルに紐づくデバイスが抜けた」PnP削除イベントを検知しても、即座にdefaultへは切り替えない。**猶予期間(デフォルト15秒、設定可能)**を置き、抜けた瞬間に1回だけ「Aが外れました。15秒以内に挿し直せばそのまま継続します」という固定文言のトースト通知を出す(動くカウントダウン表示はしない。Windows標準トーストの同一通知を秒ごとに書き換えるにはより低レベルなWinRT APIが必要で、PyInstaller化した無署名exeでの安定動作リスクが上がるため見送る)。
  - 当初5秒で運用開始したが、実機確認で「抜去検知まで1〜2秒」「挿し直し後の再検知(WMI通知)まで最大10秒程度」かかるケースが実際に観測されたため、2026-09-16に15秒へ調整した(詳細はKNOWLEDGE.mdの調整結果を参照)。
  - 猶予期間内に同じデバイスが再接続されたら、何もせずそのままAを継続(通知も出さない)。
  - 猶予期間内に**別の**登録済みデバイス(B)が挿さったら、Aの猶予は待たずに即座にBへ切り替える(③の「最後に挿さった方が勝つ」原則をそのまま適用)。
  - 猶予期間が過ぎても何も挿さらなければ、defaultへ切り替えて「defaultに切り替えました」と通知する。
- 複数デバイスが挿さっている状態で、現在のラベルに紐づかない方を抜いても何もしない(現在のラベルには影響しない)。

**事後訂正のための記録**: `events`テーブル(または記録経路)に、その打鍵がどの`device_key`・どの切替経路(手動/自動)で選ばれていたラベルの元で記録されたかを併せて保存する。VID/PID衝突等で誤ってラベル付けされていたことに後から気づいた場合、該当期間だけ遡ってラベルを訂正できるようにするための保険。

### 3. 期間サマリ(週/月/年)

- 既存の日次集計(`aggregate_daily`)と同じ考え方で、`analysis.py`に期間指定の集計関数を追加する(週=ISO週、月=年月、年=年単位でグルーピングし、速度中央値・訂正率などを同様に算出)。
- 表示場所は**既存ダッシュボード内の空きスペースに埋め込む**(ページ遷移なし)。
  - 期間種別(週/月/年)を切り替えるタブ or セレクトボックスを1つ置き、選択に応じて期間サマリ用の表・グラフ(日次と同様、速度・訂正率の推移)を切り替え表示する。
  - データ取得は`/api/summary?range=week|month|year`のようなクエリパラメータで既存の`/api/summary`を拡張する形にする。

### 4. 配布

- GitHubに**Public**新規リポジトリ`typing-activity-tracker`(アカウント: `IbushiGinjiro`)を作成し、現在の`dev/typing-activity-tracker/`の中身(データファイル`data/`とローカル専用の`config.json`は除く)をpushする。push前に`git status`で実際にstageされるファイル一覧を最終確認する(機密・個人データの混入防止)。
- README.mdを配布向けに整備。既に対応済みの「実際に入力された文字列は記録しない」の明記に加え、以下を追記する:
  - 業務PC・会社支給端末で使う場合は、情報システム部門の許可を得てから使うこと(グローバルキーフックを使う常駐アプリであるため、無許可でのインストールはセキュリティインシデント扱いされうる)
  - 同一モデルのキーボードを複数台所有している場合、USB自動検出では区別できない(手動切替を使うこと)
  - 2台同時使用には対応していない(スコープ外)
- 「実際の文字列を記録しない」ことを保証する単体テスト(`capture.py`が`key_type`分類結果以外の生データをDB・ログに一切渡さないことを確認するテスト)を配布前に追加する。
- PyInstallerで単一.exe化し、GitHub Releasesに配布用アセットとして添付する。
- コード署名は既存方針通り見送り(README等での透明性確保で対応)。

---

この内容で進めてよいか、確認をお願いします。特に2(自動検出)は新規依存(`wmi`, `winotify`)追加と設計変更を伴う一番大きな作業なので、方向性がずれていないか確認したいです。
